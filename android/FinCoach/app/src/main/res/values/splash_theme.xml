<?xml version="1.0" encoding="utf-8"?>
<!-- Путь: app/src/main/res/values/splash_theme.xml -->
<resources>
    <style name="Theme.FinCoach.Splash" parent="Theme.SplashScreen">
        <!-- Цвет фона сплэша -->
        <item name="windowSplashScreenBackground">#77D43C</item>
        <!-- Иконка на сплэше (ic_splash_icon который создадим ниже) -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <!-- После сплэша переходим в основную тему -->
        <item name="postSplashScreenTheme">@style/Theme.FinCoach</item>
    </style>
</resources>